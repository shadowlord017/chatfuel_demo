systemLog:
  destination: file
  path: "/var/log/mongodb/mongod-config.log"
  logAppend: true

net:
  port: {{ config_port }}
  bindIp: 127.0.0.1

storage:
  dbPath: /var/lib/mongodb/configdb

sharding:
  clusterRole: configsvr

replication:
   replSetName: "mongoc"
